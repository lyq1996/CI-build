language: c
dist: trusty

os:
  - linux

branches: 
  only:
    - new

before_install:
  # fetch toolchian gcc4.9
  - echo "before install"

jobs:
  # stage script can parallel.
  include:
      if: tag IS present
      env: FOO=foo

#stages:
  #- build
    
before_deploy:
  - echo "before_deploy"

deploy:
  skip_cleanup: true
  provider: releases
  token: $github_oauth_key
  on:
    tags: true
  file: 
    - hello

after_deploy:
  - echo "deploy success?"
  - echo "push to server_chan?"
